# Assembly-Language
Assembly language (often abbreviated as ASM) is a low-level programming language that provides a symbolic, human-readable representation of a computer’s machine code.  It serves as a direct interface between human programmers and the hardware
